services:
  db:
    build:
      context: ./postgres
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-cybercalc}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-cybercalc}
      POSTGRES_DB: ${DB_NAME:-cybercalc}
    networks: [database]
    mem_limit: 1g
    pids_limit: 256
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-cybercalc} -d ${DB_NAME:-cybercalc}"]
      interval: 5s
      timeout: 5s
      retries: 10
    # Порт наружу не пробрасывается намеренно — доступ к БД только из docker-сети.
    # Для локальной отладки можно временно раскомментировать:
    # ports:
    #   - "5432:5432"

  uploads-init:
    image: alpine:3.24.1@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b
    user: "0:0"
    command: ["sh", "-c", "mkdir -p /data/uploads && chown 65532:65532 /data/uploads"]
    restart: "no"
    volumes:
      - uploads_data:/data/uploads

  migrate:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}
    command: [migrate]
    restart: "no"
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_USER: ${DB_USER:-cybercalc}
      DB_PASSWORD: ${DB_PASSWORD:-cybercalc}
      DB_NAME: ${DB_NAME:-cybercalc}
      ADMIN_BOOTSTRAP_EMAIL: ${ADMIN_BOOTSTRAP_EMAIL:-admin@minc.local}
      ADMIN_BOOTSTRAP_PASSWORD: ${ADMIN_BOOTSTRAP_PASSWORD:-ChangeMe123!}
      RUNTIME_DB_USER: ${DB_RUNTIME_USER:-cybercalc_app}
      RUNTIME_DB_PASSWORD: ${DB_RUNTIME_PASSWORD:-local-only-runtime-password}
      BACKEND_FEATURE_FLAGS: ${BACKEND_FEATURE_FLAGS:-}
      FRONTEND_FEATURE_FLAGS: ${FRONTEND_FEATURE_FLAGS:-}
    read_only: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    networks: [database]
    mem_limit: 256m
    pids_limit: 64

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      uploads-init:
        condition: service_completed_successfully
      migrate:
        condition: service_completed_successfully
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: ${DB_RUNTIME_USER:-cybercalc_app}
      DB_PASSWORD: ${DB_RUNTIME_PASSWORD:-local-only-runtime-password}
      RUN_MIGRATIONS: "false"
      DB_NAME: ${DB_NAME:-cybercalc}
      DB_SSLMODE: disable
      HTTP_ADDR: ":8080"
      UPLOAD_DIR: /data/uploads
      ADMIN_BOOTSTRAP_EMAIL: ${ADMIN_BOOTSTRAP_EMAIL:-admin@minc.local}
      ADMIN_BOOTSTRAP_PASSWORD: ${ADMIN_BOOTSTRAP_PASSWORD:-ChangeMe123!}
      APP_ENV: ${APP_ENV:-development}
      PUBLIC_URL: ${PUBLIC_URL:-}
      SESSION_TTL_HOURS: ${SESSION_TTL_HOURS:-12}
      CLAMAV_ADDRESS: ${CLAMAV_ADDRESS:-}
      UPLOAD_QUOTA_BYTES: ${UPLOAD_QUOTA_BYTES:-1073741824}
      MFA_ENCRYPTION_KEY: ${MFA_ENCRYPTION_KEY:-}
      DIRECTORY_SYNC_URL: ${DIRECTORY_SYNC_URL:-}
      DIRECTORY_SYNC_INTERVAL_HOURS: ${DIRECTORY_SYNC_INTERVAL_HOURS:-24}
      DIRECTORY_ENRICH_ON_START: ${DIRECTORY_ENRICH_ON_START:-true}
      DIRECTORY_ENRICH_LIMIT: ${DIRECTORY_ENRICH_LIMIT:-0}
      DIRECTORY_ENRICH_DELAY_MS: ${DIRECTORY_ENRICH_DELAY_MS:-3000}
      BACKEND_FEATURE_FLAGS: ${BACKEND_FEATURE_FLAGS:-}
      FRONTEND_FEATURE_FLAGS: ${FRONTEND_FEATURE_FLAGS:-}
    healthcheck:
      test: ["CMD", "/app/server", "healthcheck"]
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 5
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 768m
    pids_limit: 128
    stop_grace_period: 110s
    networks: [application, database]
    volumes:
      - uploads_data:/data/uploads
    deploy:
      replicas: ${BACKEND_REPLICAS:-2}

  # Scheduled work is isolated from HTTP replicas. This service has no
  # published port and can be restarted or scaled independently from the API.
  worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}
    command: [worker]
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      uploads-init:
        condition: service_completed_successfully
      migrate:
        condition: service_completed_successfully
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: ${DB_RUNTIME_USER:-cybercalc_app}
      DB_PASSWORD: ${DB_RUNTIME_PASSWORD:-local-only-runtime-password}
      RUN_MIGRATIONS: "false"
      DB_NAME: ${DB_NAME:-cybercalc}
      DB_SSLMODE: disable
      UPLOAD_DIR: /data/uploads
      ADMIN_BOOTSTRAP_EMAIL: ${ADMIN_BOOTSTRAP_EMAIL:-admin@minc.local}
      ADMIN_BOOTSTRAP_PASSWORD: ${ADMIN_BOOTSTRAP_PASSWORD:-ChangeMe123!}
      APP_ENV: ${APP_ENV:-development}
      PUBLIC_URL: ${PUBLIC_URL:-}
      CLAMAV_ADDRESS: ${CLAMAV_ADDRESS:-}
      MFA_ENCRYPTION_KEY: ${MFA_ENCRYPTION_KEY:-}
      UPLOAD_QUOTA_BYTES: ${UPLOAD_QUOTA_BYTES:-1073741824}
      DIRECTORY_SYNC_URL: ${DIRECTORY_SYNC_URL:-}
      DIRECTORY_SYNC_INTERVAL_HOURS: ${DIRECTORY_SYNC_INTERVAL_HOURS:-24}
      DIRECTORY_ENRICH_ON_START: ${DIRECTORY_ENRICH_ON_START:-true}
      DIRECTORY_ENRICH_LIMIT: ${DIRECTORY_ENRICH_LIMIT:-0}
      DIRECTORY_ENRICH_DELAY_MS: ${DIRECTORY_ENRICH_DELAY_MS:-3000}
      BACKEND_FEATURE_FLAGS: ${BACKEND_FEATURE_FLAGS:-}
      FRONTEND_FEATURE_FLAGS: ${FRONTEND_FEATURE_FLAGS:-}
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 512m
    pids_limit: 128
    stop_grace_period: 200s
    networks: [application, database]
    volumes:
      - uploads_data:/data/uploads

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    user: "101:101"
    restart: unless-stopped
    depends_on:
      - backend
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "${HTTP_BIND:-0.0.0.0}:${HTTP_PORT:-8080}:80"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
      - /var/cache/nginx:size=256m,uid=101,gid=101
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 384m
    pids_limit: 64
    networks: [application]
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1/api/ready"]
      interval: 15s
      timeout: 5s
      retries: 5

networks:
  application:
  database:
    internal: true

volumes:
  db_data:
  uploads_data:
